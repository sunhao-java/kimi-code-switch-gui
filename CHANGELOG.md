# 变更日志

本文件记录项目的重要变更。

格式参考 Keep a Changelog，当前项目采用 `major.minor.patch` 版本号方案。

## [1.1.3] - 2026-05-09

### 修复

- 修复正式打包环境下 preload 初始化主题时 `documentElement` 可能为空导致 preload 崩溃的问题。
- 修复由 preload 崩溃引发的 `Electron preload API is unavailable` 启动失败提示。

## [1.1.2] - 2026-05-09

### 新增

- Profiles 顶部当前激活区域新增“在终端打开 Kimi”入口，可直接用当前激活 Profile 启动 CLI。
- Profiles 列表行新增悬浮终端入口，可用被点击的 Profile 生成临时配置并启动 Kimi，不改变当前激活状态。
- 设置页新增终端应用配置，支持系统终端和 iTerm2。
- README 新增多页面截图和图文功能介绍。

### 变更

- 终端启动统一使用 `kimi --config-file <配置文件>`，并为指定 Profile 写入 `~/.kimi/.panel/tmp/terminal/` 下的临时配置。
- Terminal.app 与 iTerm2 启动时改为新标签页打开，并通过粘贴加回车提升命令执行稳定性。

## [1.1.1] - 2026-05-07

### 新增

- 设置页新增快捷键管理能力，支持全局和窗口快捷键录制、启停、重置与冲突提示。
- 备份快照新增 `shortcuts.json`，恢复配置时可同步保留快捷键设置。
- 补充快捷键、Skills、更新检查和备份恢复相关 README 说明。

### 变更

- 继续拆分渲染层 `App.tsx`，将状态派生、配置持久化、快捷键绑定、备份操作、预览刷新、未保存拦截和 Tab 面板拆到独立模块。
- 拆分主进程文件访问、CLI 环境、WebDAV、快捷键和更新检查模块，降低主进程入口文件复杂度。
- 优化更新检查弹框状态和 GitHub Release 兜底检查流程。

### 修复

- 修复更新检查弹框在部分状态下的提示和版本标识展示问题。
- 修复模块拆分后页面导航、配置加载与空状态边界的潜在回归。

## [1.1.0] - 2026-04-26

### 新增

- 关于页新增 GitHub Release 更新检查能力，可根据 Homebrew、手动安装或开发构建给出不同更新提示。
- 检查到新版本后会在关于页版本号显示更新标识，直到用户完成版本升级。
- 更新检查弹框新增 Homebrew 更新命令复制、GitHub Release 跳转和 GitHub 限流提示。

### 变更

- 拆分渲染层大文件，将关于页、弹框、代码展示、概览面板、Skills 工作区、顶部快捷控件、通用布局和表单控件拆到独立模块。
- 优化外链打开安全策略，支持 `https:` 和 `mailto:` 白名单协议。

### 修复

- 修复拆分后部分图标导入缺失导致页面渲染失败的问题。
- 修复 `AppState | null` 在保存、预览刷新、Skills 刷新和未保存拦截流程中的类型边界问题。
- 修复通用设置分组无法接受条件渲染 `null` 子节点的类型问题。

## [1.0.4] - 2026-04-24

### 新增

- 状态栏托盘菜单新增语言和主题快捷切换，可直接在托盘中切换中文/English 与自动、明亮、暗色主题。
- 备份模块新增备份记录恢复能力，支持从本地目录或 WebDAV 远端备份恢复配置。

### 变更

- 优化 Skills 工作区展示，调整分页容量、详情弹窗摘要布局和技能卡片尺寸，让浏览与查看更聚焦。
- 增强 Skills frontmatter 解析，对多行描述和 block scalar 的兼容性更完整。

## [1.0.3] - 2026-04-23

### 变更

- Skills 页面重构为更聚焦的双栏工作区，支持网格/列表切换、弹窗查看技能详情，并优化右侧内容区的自适应高度。
- 设置页新增界面字体大小选项，同时补齐对新字体大小配置的读取、归一化与持久化。
- 简化 Skills 扫描来源，移除额外目录和项目目录自定义入口，统一使用自动发现策略。

### 修复

- 修复下拉组件缺失图标导入导致除首页外多个页面无法打开的运行时错误。
- 修复 Skills 页面主内容区未撑满剩余可视区域的问题。

## [1.0.2] - 2026-04-22

### 新增

- 设置页新增备份记录查看能力，支持本地目录和 WebDAV 远端备份列表展示。
- 备份记录列表新增删除操作，为后续恢复流程保留统一操作入口。

### 修复

- 修复 `config.panel.toml` 中 MCP 配置多次启用/禁用后 `extra.extra` 递归嵌套和 `enabled` 错位写入的问题。
- 修复面板配置输出时 MCP 子表头缩进异常，避免 `headers` 等字段段落格式漂移。
- 修复 MCP 导入弹框按 `Esc` 时将默认示例内容误判为已修改的问题。

### 文档

- 重写 README 项目介绍，补充 `mcp.json`、备份、状态栏快捷操作和配置查看能力说明。

## [1.0.1] - 2026-04-21

### 变更

- 优化设置页分组、顶部统计卡片、首页总览列表和自定义下拉展示样式。
- 增强 MCP 管理流程，支持 JSON 导入、启用/禁用持久化、面板保留与配置文件过滤。
- 替换应用、托盘和前端展示用品牌 Logo 资源，统一为新的透明亮色/暗色图片，并重建 macOS `icon.icns`。
- Homebrew cask 生成脚本新增 macOS 隔离属性导致的启动异常提示。

## [1.0.0] - 2026-04-20

### 修复

- 修复 GitHub Actions 发布流程中 `electron-builder` 因 tag 构建触发隐式发布而导致 macOS 和 Windows 安装包任务失败的问题。
- 调整 Release 工作流中的 GitHub Release 认证与仓库定位方式，避免 `gh release` 依赖本地 `.git` 上下文。

### 新增

- 初始版本 Electron 桌面应用，用于管理 `kimi-code-cli` 配置文件。
- Provider、Model、Profile 的可视化编辑与管理流程。
- 激活 Profile 时自动将默认项同步回 `config.toml`。
- 对 `config.toml`、`config.profiles.toml`、`config.panel.toml` 的预览支持。
- 在保存前查看配置变更 Diff。
- 中文和英文双语界面支持。
- 状态栏 / 托盘集成，并可直接切换 Profile。
- 基于记忆显示器、当前活动显示器或随机显示器的窗口打开策略。
- 关于页中的仓库、Issue 和作者链接。
- 面向 macOS 和 Windows 的 Electron Builder 安装包构建能力。
- 基于 `v*` 标签触发的 GitHub Actions 发布流水线。

### 测试

- 为共享配置存储逻辑补充了 Vitest 覆盖。
